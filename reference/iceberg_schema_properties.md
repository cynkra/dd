# DuckDB function iceberg_schema_properties

DuckDB function `iceberg_schema_properties()`.

## Usage

``` r
iceberg_schema_properties(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
