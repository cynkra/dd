# DuckDB function hnsw_compact_index

DuckDB function `hnsw_compact_index()`.

## Usage

``` r
hnsw_compact_index(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `vss` extension (`LOAD vss;`).
