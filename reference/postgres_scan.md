# DuckDB function postgres_scan

DuckDB function `postgres_scan()`.

## Usage

``` r
postgres_scan(col0, col1, col2)
```

## Arguments

- col0, col1, col2:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `postgres_scanner` extension (`LOAD postgres_scanner;`).
