# DuckDB function sqlite_query

DuckDB function `sqlite_query()`.

## Usage

``` r
sqlite_query(col0, col1, params)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- params:

  `ANY`

## Value

Unspecified.

## Provided by

The `sqlite_scanner` extension (`LOAD sqlite_scanner;`).
