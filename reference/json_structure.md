# DuckDB function json_structure

DuckDB function `json_structure()`.

## Usage

``` r
json_structure(col0)
```

## Arguments

- col0:

  `VARCHAR | JSON`

## Value

`JSON`

## Provided by

The `json` extension (`LOAD json;`).
