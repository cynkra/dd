# DuckDB function parquet_schema

DuckDB function `parquet_schema()`.

## Arguments

- col0:

  `VARCHAR | VARCHAR[]`

## Value

Unspecified.

## Overloads

- `parquet_schema(col0 = VARCHAR)`

- `` parquet_schema(col0 = `VARCHAR[]`) ``
