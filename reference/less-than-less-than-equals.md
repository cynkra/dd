# DuckDB function \<\<=

DuckDB function `<<=()`.

## Usage

``` r
`<<=`(col0, col1)
```

## Arguments

- col0, col1:

  `INET`

## Value

`BOOLEAN`

## Provided by

The `inet` extension (`LOAD inet;`).
