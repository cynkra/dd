# DuckDB function icu_collate_ms

DuckDB function `icu_collate_ms()`.

## Usage

``` r
icu_collate_ms(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

`VARCHAR`

## Provided by

The `icu` extension (`LOAD icu;`).
