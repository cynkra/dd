# DuckDB function ducklake_rewrite_data_files

DuckDB function `ducklake_rewrite_data_files()`.

## Usage

``` r
ducklake_rewrite_data_files(col0, col1, schema, delete_threshold)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- schema:

  `VARCHAR`

- delete_threshold:

  `DOUBLE`

## Value

Unspecified.

## Overloads

- `ducklake_rewrite_data_files(col0 = VARCHAR, col1 = VARCHAR, schema = VARCHAR, delete_threshold = DOUBLE)`

- `ducklake_rewrite_data_files(col0 = VARCHAR, delete_threshold = DOUBLE)`

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
