# DuckDB function json_merge_patch

DuckDB function `json_merge_patch()`.

## Usage

``` r
json_merge_patch(col0, col1)
```

## Arguments

- col0, col1:

  `JSON`

## Value

`JSON`

## Provided by

The `json` extension (`LOAD json;`).
