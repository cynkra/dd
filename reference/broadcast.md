# DuckDB function broadcast

DuckDB function `broadcast()`.

## Usage

``` r
broadcast(col0)
```

## Arguments

- col0:

  `INET`

## Value

`INET`

## Provided by

The `inet` extension (`LOAD inet;`).
