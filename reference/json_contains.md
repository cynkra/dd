# DuckDB function json_contains

DuckDB function `json_contains()`.

## Usage

``` r
json_contains(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR | JSON`

## Value

`BOOLEAN`

## Provided by

The `json` extension (`LOAD json;`).
