# DuckDB function delta_get_transaction_version

DuckDB function `delta_get_transaction_version()`.

## Usage

``` r
delta_get_transaction_version(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `delta` extension (`LOAD delta;`).
