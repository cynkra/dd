# DuckDB function postgres_attach

DuckDB function `postgres_attach()`.

## Usage

``` r
postgres_attach(col0, suffix, sink_schema, filter_pushdown, source_schema, overwrite)
```

## Arguments

- col0:

  `VARCHAR`

- suffix:

  `VARCHAR`

- sink_schema:

  `VARCHAR`

- filter_pushdown:

  `BOOLEAN`

- source_schema:

  `VARCHAR`

- overwrite:

  `BOOLEAN`

## Value

Unspecified.

## Provided by

The `postgres_scanner` extension (`LOAD postgres_scanner;`).
