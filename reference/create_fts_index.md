# DuckDB function create_fts_index

DuckDB function `create_fts_index()`.

## Usage

``` r
create_fts_index(col0, col1, overwrite, lower, ignore, stopwords, strip_accents, stemmer)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- overwrite:

  `BOOLEAN`

- lower:

  `BOOLEAN`

- ignore:

  `VARCHAR`

- stopwords:

  `VARCHAR`

- strip_accents:

  `BOOLEAN`

- stemmer:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `fts` extension (`LOAD fts;`).
