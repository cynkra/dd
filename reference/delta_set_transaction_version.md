# DuckDB function delta_set_transaction_version

DuckDB function `delta_set_transaction_version()`.

## Usage

``` r
delta_set_transaction_version(col0, col1, col2, col3)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- col2:

  `UBIGINT`

- col3:

  `UBIGINT`

## Value

Unspecified.

## Provided by

The `delta` extension (`LOAD delta;`).
