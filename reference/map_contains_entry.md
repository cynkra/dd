# DuckDB function map_contains_entry

DuckDB macro `map_contains_entry()`.

## Usage

``` r
map_contains_entry(map, key, value)
```

## Arguments

- map, key, value:

  Unspecified.

## Value

Unspecified.
