# DuckDB function stem

DuckDB function `stem()`.

## Usage

``` r
stem(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR`

## Value

`VARCHAR`

## Provided by

The `fts` extension (`LOAD fts;`).
