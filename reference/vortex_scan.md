# DuckDB function vortex_scan

DuckDB function `vortex_scan()`.

## Usage

``` r
vortex_scan(col0)
```

## Arguments

- col0:

  `VARCHAR | VARCHAR[]`

## Value

Unspecified.

## Provided by

The `vortex` extension (`LOAD vortex;`).
