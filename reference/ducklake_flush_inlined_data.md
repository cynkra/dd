# DuckDB function ducklake_flush_inlined_data

DuckDB function `ducklake_flush_inlined_data()`.

## Usage

``` r
ducklake_flush_inlined_data(col0, table_name, schema_name)
```

## Arguments

- col0, table_name, schema_name:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
