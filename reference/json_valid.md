# DuckDB function json_valid

DuckDB function `json_valid()`.

## Usage

``` r
json_valid(col0)
```

## Arguments

- col0:

  `VARCHAR | JSON`

## Value

`BOOLEAN`

## Provided by

The `json` extension (`LOAD json;`).
