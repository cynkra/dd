# DuckDB function set_iceberg_schema_properties

DuckDB function `set_iceberg_schema_properties()`.

## Usage

``` r
set_iceberg_schema_properties(col0, col1)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `ANY`

## Value

Unspecified.

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
