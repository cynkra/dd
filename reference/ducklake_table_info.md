# DuckDB function ducklake_table_info

DuckDB function `ducklake_table_info()`.

## Usage

``` r
ducklake_table_info(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
