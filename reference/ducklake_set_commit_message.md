# DuckDB function ducklake_set_commit_message

DuckDB function `ducklake_set_commit_message()`.

## Usage

``` r
ducklake_set_commit_message(col0, col1, col2, extra_info)
```

## Arguments

- col0, col1, col2, extra_info:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
