# DuckDB function drop_fts_index

DuckDB function `drop_fts_index()`.

## Usage

``` r
drop_fts_index(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `fts` extension (`LOAD fts;`).
