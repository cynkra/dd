# DuckDB function postgres_query

DuckDB function `postgres_query()`.

## Usage

``` r
postgres_query(col0, col1, params, use_transaction)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- params:

  `ANY`

- use_transaction:

  `BOOLEAN`

## Value

Unspecified.

## Provided by

The `postgres_scanner` extension (`LOAD postgres_scanner;`).
