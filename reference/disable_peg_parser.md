# DuckDB function disable_peg_parser

DuckDB function `disable_peg_parser()`.

## Usage

``` r
disable_peg_parser()
```

## Value

Unspecified.

## Provided by

The `autocomplete` extension (`LOAD autocomplete;`).
