# DuckDB function parquet_bloom_probe

DuckDB function `parquet_bloom_probe()`.

## Arguments

- col0:

  `VARCHAR | VARCHAR[]`

- col1:

  `VARCHAR`

- col2:

  `ANY`

## Value

Unspecified.

## Overloads

- `parquet_bloom_probe(col0 = VARCHAR, col1 = VARCHAR, col2 = ANY)`

- `` parquet_bloom_probe(col0 = `VARCHAR[]`, col1 = VARCHAR, col2 = ANY) ``
