# DuckDB function check_peg_parser

DuckDB function `check_peg_parser()`.

## Usage

``` r
check_peg_parser(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `autocomplete` extension (`LOAD autocomplete;`).
