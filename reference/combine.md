# DuckDB function combine

DuckDB function `combine()`.

## Usage

``` r
combine(col0 = `AGGREGATE_STATE<?>`, col1 = ANY)
```

## Arguments

- col0:

  `AGGREGATE_STATE<?>`

- col1:

  `ANY`

## Value

`AGGREGATE_STATE<?>`
