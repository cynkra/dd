# DuckDB function remove_iceberg_schema_properties

DuckDB function `remove_iceberg_schema_properties()`.

## Usage

``` r
remove_iceberg_schema_properties(col0, col1)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR[]`

## Value

Unspecified.

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
