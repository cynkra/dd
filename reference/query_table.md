# DuckDB function query_table

DuckDB function `query_table()`.

## Arguments

- col0:

  `VARCHAR | VARCHAR[]`

- col1:

  `BOOLEAN`

## Value

Unspecified.

## Overloads

- `query_table(col0 = VARCHAR)`

- `` query_table(col0 = `VARCHAR[]`) ``

- `` query_table(col0 = `VARCHAR[]`, col1 = BOOLEAN) ``
