# DuckDB function ducklake_options

DuckDB function `ducklake_options()`.

## Usage

``` r
ducklake_options(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
