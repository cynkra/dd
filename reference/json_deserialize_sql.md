# DuckDB function json_deserialize_sql

DuckDB function `json_deserialize_sql()`.

## Usage

``` r
json_deserialize_sql(col0)
```

## Arguments

- col0:

  `JSON`

## Value

`VARCHAR`

## Provided by

The `json` extension (`LOAD json;`).
