# DuckDB function sqlite_scan

DuckDB function `sqlite_scan()`.

## Usage

``` r
sqlite_scan(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `sqlite_scanner` extension (`LOAD sqlite_scanner;`).
