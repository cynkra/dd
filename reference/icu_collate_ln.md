# DuckDB function icu_collate_ln

DuckDB function `icu_collate_ln()`.

## Usage

``` r
icu_collate_ln(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

`VARCHAR`

## Provided by

The `icu` extension (`LOAD icu;`).
