# DuckDB function count

Returns the number of non-NULL values in arg.

## Usage

``` r
count(arg)
```

## Arguments

- arg:

  `ANY`

## Value

`BIGINT`

## Overloads

- `count(arg = ANY)`

- `count()`

## SQL examples

    count(A)
