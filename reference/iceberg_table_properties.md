# DuckDB function iceberg_table_properties

DuckDB function `iceberg_table_properties()`.

## Usage

``` r
iceberg_table_properties(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
