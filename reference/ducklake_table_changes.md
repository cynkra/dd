# DuckDB function ducklake_table_changes

DuckDB macro `ducklake_table_changes()`.

## Usage

``` r
ducklake_table_changes(catalog, schema_name, table_name, start_snapshot, end_snapshot)
```

## Arguments

- catalog, schema_name, table_name, start_snapshot, end_snapshot:

  Unspecified.

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
