# DuckDB function iceberg_truncate

DuckDB function `iceberg_truncate()`.

## Usage

``` r
iceberg_truncate(col0, col1)
```

## Arguments

- col0:

  `INTEGER`

- col1:

  `INTEGER | BIGINT | VARCHAR | BLOB | DECIMAL`

## Value

`INTEGER | BIGINT | VARCHAR | BLOB | DECIMAL`

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
