# DuckDB function pg_clear_cache

DuckDB function `pg_clear_cache()`.

## Usage

``` r
pg_clear_cache()
```

## Value

Unspecified.

## Provided by

The `postgres_scanner` extension (`LOAD postgres_scanner;`).
