# DuckDB function ST_Point

Creates a GEOMETRY point.

## Usage

``` r
ST_Point(x, y)
```

## Arguments

- x, y:

  `DOUBLE`

## Value

`GEOMETRY`

## Provided by

The `spatial` extension (`LOAD spatial;`).
