# DuckDB function json_transform

DuckDB function `json_transform()`.

## Usage

``` r
json_transform(col0, col1)
```

## Arguments

- col0:

  `VARCHAR | JSON`

- col1:

  `VARCHAR`

## Value

`ANY`

## Provided by

The `json` extension (`LOAD json;`).
