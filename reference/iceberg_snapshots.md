# DuckDB function iceberg_snapshots

DuckDB function `iceberg_snapshots()`.

## Usage

``` r
iceberg_snapshots(col0, version_name_format, version, metadata_compression_codec)
```

## Arguments

- col0, version_name_format, version, metadata_compression_codec:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
