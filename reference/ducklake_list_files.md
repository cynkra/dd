# DuckDB function ducklake_list_files

DuckDB function `ducklake_list_files()`.

## Usage

``` r
ducklake_list_files(col0, col1, snapshot_time, snapshot_version, schema)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- snapshot_time:

  `TIMESTAMP WITH TIME ZONE`

- snapshot_version:

  `BIGINT`

- schema:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
