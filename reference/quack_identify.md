# DuckDB function quack_identify

DuckDB function `quack_identify()`.

## Usage

``` r
quack_identify(meta, region, hostname, provider, name)
```

## Arguments

- meta, region, hostname, provider, name:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `quack` extension (`LOAD quack;`).
