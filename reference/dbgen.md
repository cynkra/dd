# DuckDB function dbgen

DuckDB function `dbgen()`.

## Usage

``` r
dbgen(step, suffix, schema, catalog, overwrite, children, sf)
```

## Arguments

- step:

  `UINTEGER`

- suffix:

  `VARCHAR`

- schema:

  `VARCHAR`

- catalog:

  `VARCHAR`

- overwrite:

  `BOOLEAN`

- children:

  `UINTEGER`

- sf:

  `DOUBLE`

## Value

Unspecified.

## Provided by

The `tpch` extension (`LOAD tpch;`).
