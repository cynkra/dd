# DuckDB function quack_query_by_name

DuckDB function `quack_query_by_name()`.

## Usage

``` r
quack_query_by_name(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `quack` extension (`LOAD quack;`).
