# DuckDB function parquet_metadata

DuckDB function `parquet_metadata()`.

## Arguments

- col0:

  `VARCHAR | VARCHAR[]`

## Value

Unspecified.

## Overloads

- `parquet_metadata(col0 = VARCHAR)`

- `` parquet_metadata(col0 = `VARCHAR[]`) ``
