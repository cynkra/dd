# DuckDB function ST_IsClosed

Check if a geometry is 'closed'.

## Usage

``` r
ST_IsClosed(geom)
```

## Arguments

- geom:

  `GEOMETRY`

## Value

`BOOLEAN`

## Provided by

The `spatial` extension (`LOAD spatial;`).
