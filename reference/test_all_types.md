# DuckDB function test_all_types

DuckDB function `test_all_types()`.

## Usage

``` r
test_all_types(use_large_bignum, use_large_enum)
```

## Arguments

- use_large_bignum, use_large_enum:

  `BOOLEAN`

## Value

Unspecified.
