# DuckDB function test_all_types

DuckDB function `test_all_types()`.

## Usage

``` r
test_all_types(use_large_enum = BOOLEAN, use_large_bignum = BOOLEAN)
```

## Arguments

- use_large_enum:

  `BOOLEAN`

- use_large_bignum:

  `BOOLEAN`

## Value

Unspecified.
