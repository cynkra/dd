# DuckDB function ducklake_snapshots

DuckDB function `ducklake_snapshots()`.

## Usage

``` r
ducklake_snapshots(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
