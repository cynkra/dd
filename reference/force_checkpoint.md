# DuckDB function force_checkpoint

DuckDB function `force_checkpoint()`.

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Overloads

- `force_checkpoint()`

- `force_checkpoint(col0 = VARCHAR)`

- `force_checkpoint()`
