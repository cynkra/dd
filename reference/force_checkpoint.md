# DuckDB function force_checkpoint

DuckDB function `force_checkpoint()`.

## Usage

``` r
force_checkpoint()
```

## Value

Unspecified.

## Overloads

- `force_checkpoint()`

- `force_checkpoint(col0 = VARCHAR)`
