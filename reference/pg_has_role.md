# DuckDB function pg_has_role

DuckDB function `pg_has_role()`.

## Usage

``` r
pg_has_role(user, role, privilege)
```

## Arguments

- user, role, privilege:

  Unspecified.

## Value

Unspecified.

## Overloads

- `pg_has_role(user, role, privilege)`

- `pg_has_role(role, privilege)`
