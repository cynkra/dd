# DuckDB function from_json

DuckDB function `from_json()`.

## Usage

``` r
from_json(col0, col1)
```

## Arguments

- col0:

  `VARCHAR | JSON`

- col1:

  `VARCHAR`

## Value

`ANY`

## Provided by

The `json` extension (`LOAD json;`).
