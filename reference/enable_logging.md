# DuckDB function enable_logging

DuckDB function `enable_logging()`.

## Usage

``` r
enable_logging(
  storage_buffer_size,
  storage_normalize,
  storage_path,
  storage_config,
  storage,
  level
)
```

## Arguments

- storage_buffer_size:

  `UBIGINT`

- storage_normalize:

  `BOOLEAN`

- storage_path:

  `VARCHAR`

- storage_config:

  `ANY`

- storage:

  `VARCHAR`

- level:

  `VARCHAR`

## Value

Unspecified.
