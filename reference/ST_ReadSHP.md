# DuckDB function ST_ReadSHP

Read a Shapefile without relying on the GDAL library.

## Usage

``` r
ST_ReadSHP(col0, encoding)
```

## Arguments

- col0, encoding:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `spatial` extension (`LOAD spatial;`).
