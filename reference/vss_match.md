# DuckDB function vss_match

DuckDB macro `vss_match()`.

## Usage

``` r
vss_match(right_table, left_col, right_col, k, metric)
```

## Arguments

- right_table, left_col, right_col, k, metric:

  Unspecified.

## Value

Unspecified.

## Provided by

The `vss` extension (`LOAD vss;`).
