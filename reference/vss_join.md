# DuckDB function vss_join

DuckDB macro `vss_join()`.

## Usage

``` r
vss_join(left_table, right_table, left_col, right_col, k, metric)
```

## Arguments

- left_table, right_table, left_col, right_col, k, metric:

  Unspecified.

## Value

Unspecified.

## Provided by

The `vss` extension (`LOAD vss;`).
