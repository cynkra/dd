# DuckDB function ST_Boundary

Returns the "boundary" of a geometry.

## Usage

``` r
ST_Boundary(geom)
```

## Arguments

- geom:

  `GEOMETRY`

## Value

`GEOMETRY`

## Provided by

The `spatial` extension (`LOAD spatial;`).
