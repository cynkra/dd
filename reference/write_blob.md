# DuckDB function write_blob

DuckDB function `write_blob()`.

## Usage

``` r
write_blob(col0, col1)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `BLOB`

## Value

`BOOLEAN`

## Provided by

The `delta` extension (`LOAD delta;`).
