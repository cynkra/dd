# DuckDB function read_vortex

DuckDB function `read_vortex()`.

## Usage

``` r
read_vortex(col0)
```

## Arguments

- col0:

  `VARCHAR | VARCHAR[]`

## Value

Unspecified.

## Provided by

The `vortex` extension (`LOAD vortex;`).
