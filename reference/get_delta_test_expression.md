# DuckDB function get_delta_test_expression

DuckDB function `get_delta_test_expression()`.

## Usage

``` r
get_delta_test_expression()
```

## Value

`VARCHAR[]`

## Provided by

The `delta` extension (`LOAD delta;`).
