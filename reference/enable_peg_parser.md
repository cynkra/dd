# DuckDB function enable_peg_parser

DuckDB function `enable_peg_parser()`.

## Usage

``` r
enable_peg_parser()
```

## Value

Unspecified.

## Provided by

The `autocomplete` extension (`LOAD autocomplete;`).
