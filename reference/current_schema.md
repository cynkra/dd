# DuckDB function current_schema

Returns the name of the currently active schema. Default is main.

## Value

`VARCHAR`

## Overloads

- `current_schema()`

- `current_schema()`

## SQL examples

    current_schema()
