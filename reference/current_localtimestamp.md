# DuckDB function current_localtimestamp

DuckDB function `current_localtimestamp()`.

## Usage

``` r
current_localtimestamp()
```

## Value

`TIMESTAMP`

## Provided by

The `icu` extension (`LOAD icu;`).
