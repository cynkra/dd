# DuckDB function icu_sort_key

DuckDB function `icu_sort_key()`.

## Usage

``` r
icu_sort_key(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR`

## Value

`VARCHAR`

## Provided by

The `icu` extension (`LOAD icu;`).
