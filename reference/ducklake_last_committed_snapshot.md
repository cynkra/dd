# DuckDB function ducklake_last_committed_snapshot

DuckDB function `ducklake_last_committed_snapshot()`.

## Usage

``` r
ducklake_last_committed_snapshot(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
