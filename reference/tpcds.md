# DuckDB function tpcds

DuckDB function `tpcds()`.

## Usage

``` r
tpcds(col0)
```

## Arguments

- col0:

  `BIGINT`

## Value

Unspecified.

## Provided by

The `tpcds` extension (`LOAD tpcds;`).
