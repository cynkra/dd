# DuckDB function quack_check_token

DuckDB function `quack_check_token()`.

## Usage

``` r
quack_check_token(col0, col1, col2)
```

## Arguments

- col0, col1, col2:

  `VARCHAR`

## Value

`BOOLEAN`

## Provided by

The `quack` extension (`LOAD quack;`).
