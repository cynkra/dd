# DuckDB function parse_delta_filter_logline

DuckDB macro `parse_delta_filter_logline()`.

## Usage

``` r
parse_delta_filter_logline(x)
```

## Arguments

- x:

  Unspecified.

## Value

Unspecified.

## Provided by

The `delta` extension (`LOAD delta;`).
