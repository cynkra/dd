# DuckDB function json_exists

DuckDB function `json_exists()`.

## Usage

``` r
json_exists(col0, col1)
```

## Arguments

- col0:

  `VARCHAR | JSON`

- col1:

  `VARCHAR | VARCHAR[]`

## Value

`BOOLEAN | BOOLEAN[]`

## Provided by

The `json` extension (`LOAD json;`).
