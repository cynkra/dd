# DuckDB function rtree_index_dump

DuckDB function `rtree_index_dump()`.

## Usage

``` r
rtree_index_dump(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `spatial` extension (`LOAD spatial;`).
