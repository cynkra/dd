# DuckDB function ST_IsValid

Returns true if the geometry is valid.

## Usage

``` r
ST_IsValid(geom)
```

## Arguments

- geom:

  `GEOMETRY`

## Value

`BOOLEAN`

## Provided by

The `spatial` extension (`LOAD spatial;`).
