# DuckDB function array_agg

Returns a LIST containing all the values of a column.

## Usage

``` r
array_agg(arg)
```

## Arguments

- arg:

  `T`

## Value

`T[]`

## SQL examples

    array_agg(A)
    list(A)
