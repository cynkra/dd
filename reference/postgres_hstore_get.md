# DuckDB function postgres_hstore_get

DuckDB function `postgres_hstore_get()`.

## Usage

``` r
postgres_hstore_get(col0, col1)
```

## Arguments

- col0, col1:

  `VARCHAR`

## Value

`VARCHAR`

## Provided by

The `postgres_scanner` extension (`LOAD postgres_scanner;`).
