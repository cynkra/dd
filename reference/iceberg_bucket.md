# DuckDB function iceberg_bucket

DuckDB function `iceberg_bucket()`.

## Usage

``` r
iceberg_bucket(col0, col1)
```

## Arguments

- col0:

  `INTEGER`

- col1:

  `INTEGER | BIGINT | VARCHAR | BLOB | DATE | TIMESTAMP | TIMESTAMP WITH TIME ZONE | TIMESTAMP_NS | TIME | UUID | DECIMAL`

## Value

`INTEGER`

## Provided by

The `iceberg` extension (`LOAD iceberg;`).
