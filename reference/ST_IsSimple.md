# DuckDB function ST_IsSimple

Returns true if the geometry is simple.

## Usage

``` r
ST_IsSimple(geom)
```

## Arguments

- geom:

  `GEOMETRY`

## Value

`BOOLEAN`

## Provided by

The `spatial` extension (`LOAD spatial;`).
