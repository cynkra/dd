# DuckDB function ducklake_settings

DuckDB function `ducklake_settings()`.

## Usage

``` r
ducklake_settings(col0)
```

## Arguments

- col0:

  `VARCHAR`

## Value

Unspecified.

## Provided by

The `ducklake` extension (`LOAD ducklake;`).
