# DuckDB function quack_query

DuckDB function `quack_query()`.

## Usage

``` r
quack_query(col0, col1, token, disable_ssl)
```

## Arguments

- col0:

  `VARCHAR`

- col1:

  `VARCHAR`

- token:

  `VARCHAR`

- disable_ssl:

  `BOOLEAN`

## Value

Unspecified.

## Provided by

The `quack` extension (`LOAD quack;`).
