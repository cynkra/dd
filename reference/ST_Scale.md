# DuckDB function ST_Scale

DuckDB function `ST_Scale()`.

## Usage

``` r
ST_Scale(geom, xs, ys, zs)
```

## Arguments

- geom, xs, ys, zs:

  Unspecified.

## Value

Unspecified.

## Overloads

- `ST_Scale(geom, xs, ys, zs)`

- `ST_Scale(geom, xs, ys)`

## Provided by

The `spatial` extension (`LOAD spatial;`).
