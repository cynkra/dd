# DuckDB function dsdgen

DuckDB function `dsdgen()`.

## Usage

``` r
dsdgen(suffix, schema, catalog, keys, overwrite, sf)
```

## Arguments

- suffix:

  `VARCHAR`

- schema:

  `VARCHAR`

- catalog:

  `VARCHAR`

- keys:

  `BOOLEAN`

- overwrite:

  `BOOLEAN`

- sf:

  `DOUBLE`

## Value

Unspecified.

## Provided by

The `tpcds` extension (`LOAD tpcds;`).
