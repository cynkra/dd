# DuckDB function copy_dir

DuckDB macro `copy_dir()`.

## Usage

``` r
copy_dir(src_dir, dst_dir)
```

## Arguments

- src_dir, dst_dir:

  Unspecified.

## Value

Unspecified.

## Provided by

The `delta` extension (`LOAD delta;`).
