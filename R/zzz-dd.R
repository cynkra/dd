#' DuckDB functions
#'
#' A list of known DuckDB functions.
#'
#' @export
#' @examples
#' dd[1:3]
dd <- base::list(
  `!__postfix` = `!__postfix`,
  `!~~` = `!~~`,
  `!~~*` = `!~~*`,
  `%` = `%`,
  `&` = `&`,
  `&&` = `&&`,
  `*` = `*`,
  `**` = `**`,
  `->>` = `->>`,
  `/` = `/`,
  `//` = `//`,
  `<<` = `<<`,
  `<<=` = `<<=`,
  `<=>` = `<=>`,
  `<@` = `<@`,
  `>>` = `>>`,
  `>>=` = `>>=`,
  `@` = `@`,
  `@>` = `@>`,
  DuckDB_PROJ_Compiled_Version = DuckDB_PROJ_Compiled_Version,
  DuckDB_Proj_Version = DuckDB_Proj_Version,
  ST_Affine = ST_Affine,
  ST_Area = ST_Area,
  ST_Area_Spheroid = ST_Area_Spheroid,
  ST_AsGeoJSON = ST_AsGeoJSON,
  ST_AsHEXWKB = ST_AsHEXWKB,
  ST_AsMVT = ST_AsMVT,
  ST_AsMVTGeom = ST_AsMVTGeom,
  ST_AsSVG = ST_AsSVG,
  ST_Azimuth = ST_Azimuth,
  ST_Boundary = ST_Boundary,
  ST_Buffer = ST_Buffer,
  ST_BuildArea = ST_BuildArea,
  ST_Centroid = ST_Centroid,
  ST_ClosestPoint = ST_ClosestPoint,
  ST_Collect = ST_Collect,
  ST_CollectionExtract = ST_CollectionExtract,
  ST_ConcaveHull = ST_ConcaveHull,
  ST_Contains = ST_Contains,
  ST_ContainsProperly = ST_ContainsProperly,
  ST_ConvexHull = ST_ConvexHull,
  ST_CoverageClean = ST_CoverageClean,
  ST_CoverageInvalidEdges = ST_CoverageInvalidEdges,
  ST_CoverageInvalidEdges_Agg = ST_CoverageInvalidEdges_Agg,
  ST_CoverageSimplify = ST_CoverageSimplify,
  ST_CoverageSimplify_Agg = ST_CoverageSimplify_Agg,
  ST_CoverageUnion = ST_CoverageUnion,
  ST_CoverageUnion_Agg = ST_CoverageUnion_Agg,
  ST_CoveredBy = ST_CoveredBy,
  ST_Covers = ST_Covers,
  ST_Crosses = ST_Crosses,
  ST_DWithin = ST_DWithin,
  ST_DWithin_GEOS = ST_DWithin_GEOS,
  ST_DWithin_Spheroid = ST_DWithin_Spheroid,
  ST_Difference = ST_Difference,
  ST_Dimension = ST_Dimension,
  ST_Disjoint = ST_Disjoint,
  ST_Distance = ST_Distance,
  ST_Distance_GEOS = ST_Distance_GEOS,
  ST_Distance_Sphere = ST_Distance_Sphere,
  ST_Distance_Spheroid = ST_Distance_Spheroid,
  ST_Drivers = ST_Drivers,
  ST_Dump = ST_Dump,
  ST_EndPoint = ST_EndPoint,
  ST_Envelope = ST_Envelope,
  ST_Envelope_Agg = ST_Envelope_Agg,
  ST_Equals = ST_Equals,
  ST_Expand = ST_Expand,
  ST_Extent = ST_Extent,
  ST_Extent_Agg = ST_Extent_Agg,
  ST_Extent_Approx = ST_Extent_Approx,
  ST_ExteriorRing = ST_ExteriorRing,
  ST_FlipCoordinates = ST_FlipCoordinates,
  ST_Force2D = ST_Force2D,
  ST_Force3DM = ST_Force3DM,
  ST_Force3DZ = ST_Force3DZ,
  ST_Force4D = ST_Force4D,
  ST_GeneratePoints = ST_GeneratePoints,
  ST_GeomFromGeoJSON = ST_GeomFromGeoJSON,
  ST_GeomFromHEXEWKB = ST_GeomFromHEXEWKB,
  ST_GeomFromHEXWKB = ST_GeomFromHEXWKB,
  ST_GeomFromText = ST_GeomFromText,
  ST_GeometryType = ST_GeometryType,
  ST_HasM = ST_HasM,
  ST_HasZ = ST_HasZ,
  ST_Hilbert = ST_Hilbert,
  ST_InteriorRingN = ST_InteriorRingN,
  ST_InterpolatePoint = ST_InterpolatePoint,
  ST_Intersection = ST_Intersection,
  ST_Intersection_Agg = ST_Intersection_Agg,
  ST_Intersects = ST_Intersects,
  ST_IsClosed = ST_IsClosed,
  ST_IsEmpty = ST_IsEmpty,
  ST_IsRing = ST_IsRing,
  ST_IsSimple = ST_IsSimple,
  ST_IsValid = ST_IsValid,
  ST_Length = ST_Length,
  ST_Length_Spheroid = ST_Length_Spheroid,
  ST_LineInterpolatePoint = ST_LineInterpolatePoint,
  ST_LineInterpolatePoints = ST_LineInterpolatePoints,
  ST_LineLocatePoint = ST_LineLocatePoint,
  ST_LineMerge = ST_LineMerge,
  ST_LineString2DFromWKB = ST_LineString2DFromWKB,
  ST_LineSubstring = ST_LineSubstring,
  ST_LocateAlong = ST_LocateAlong,
  ST_LocateBetween = ST_LocateBetween,
  ST_M = ST_M,
  ST_MMax = ST_MMax,
  ST_MMin = ST_MMin,
  ST_MakeBox2D = ST_MakeBox2D,
  ST_MakeEnvelope = ST_MakeEnvelope,
  ST_MakeLine = ST_MakeLine,
  ST_MakePoint = ST_MakePoint,
  ST_MakePolygon = ST_MakePolygon,
  ST_MakeValid = ST_MakeValid,
  ST_MaximumInscribedCircle = ST_MaximumInscribedCircle,
  ST_MemUnion_Agg = ST_MemUnion_Agg,
  ST_MinimumRotatedRectangle = ST_MinimumRotatedRectangle,
  ST_Multi = ST_Multi,
  ST_NGeometries = ST_NGeometries,
  ST_NInteriorRings = ST_NInteriorRings,
  ST_NPoints = ST_NPoints,
  ST_Node = ST_Node,
  ST_Normalize = ST_Normalize,
  ST_NumGeometries = ST_NumGeometries,
  ST_NumInteriorRings = ST_NumInteriorRings,
  ST_NumPoints = ST_NumPoints,
  ST_Overlaps = ST_Overlaps,
  ST_Perimeter = ST_Perimeter,
  ST_Perimeter_Spheroid = ST_Perimeter_Spheroid,
  ST_Point = ST_Point,
  ST_Point2D = ST_Point2D,
  ST_Point2DFromWKB = ST_Point2DFromWKB,
  ST_Point3D = ST_Point3D,
  ST_Point4D = ST_Point4D,
  ST_PointN = ST_PointN,
  ST_PointOnSurface = ST_PointOnSurface,
  ST_Points = ST_Points,
  ST_Polygon2DFromWKB = ST_Polygon2DFromWKB,
  ST_Polygonize = ST_Polygonize,
  ST_QuadKey = ST_QuadKey,
  ST_Read = ST_Read,
  ST_ReadOSM = ST_ReadOSM,
  ST_ReadSHP = ST_ReadSHP,
  ST_Read_Meta = ST_Read_Meta,
  ST_ReducePrecision = ST_ReducePrecision,
  ST_RemoveRepeatedPoints = ST_RemoveRepeatedPoints,
  ST_Reverse = ST_Reverse,
  ST_Rotate = ST_Rotate,
  ST_RotateX = ST_RotateX,
  ST_RotateY = ST_RotateY,
  ST_RotateZ = ST_RotateZ,
  ST_Scale = ST_Scale,
  ST_ShortestLine = ST_ShortestLine,
  ST_Simplify = ST_Simplify,
  ST_SimplifyPreserveTopology = ST_SimplifyPreserveTopology,
  ST_Snap = ST_Snap,
  ST_StartPoint = ST_StartPoint,
  ST_SymDifference = ST_SymDifference,
  ST_TileEnvelope = ST_TileEnvelope,
  ST_Touches = ST_Touches,
  ST_TransScale = ST_TransScale,
  ST_Transform = ST_Transform,
  ST_Translate = ST_Translate,
  ST_Union = ST_Union,
  ST_Union_Agg = ST_Union_Agg,
  ST_VoronoiDiagram = ST_VoronoiDiagram,
  ST_Within = ST_Within,
  ST_WithinProperly = ST_WithinProperly,
  ST_X = ST_X,
  ST_XMax = ST_XMax,
  ST_XMin = ST_XMin,
  ST_Y = ST_Y,
  ST_YMax = ST_YMax,
  ST_YMin = ST_YMin,
  ST_Z = ST_Z,
  ST_ZMFlag = ST_ZMFlag,
  ST_ZMax = ST_ZMax,
  ST_ZMin = ST_ZMin,
  `^` = `^`,
  `^@` = `^@`,
  abs = abs,
  acos = acos,
  acosh = acosh,
  add = add,
  add_parquet_key = add_parquet_key,
  age = age,
  aggregate = aggregate,
  ago = ago,
  alias = alias,
  all_profiling_output = all_profiling_output,
  any_value = any_value,
  apply = apply,
  approx_count_distinct = approx_count_distinct,
  approx_quantile = approx_quantile,
  approx_top_k = approx_top_k,
  arbitrary = arbitrary,
  arg_max = arg_max,
  arg_max_null = arg_max_null,
  arg_max_nulls_last = arg_max_nulls_last,
  arg_min = arg_min,
  arg_min_null = arg_min_null,
  arg_min_nulls_last = arg_min_nulls_last,
  argmax = argmax,
  argmin = argmin,
  array_agg = array_agg,
  array_aggr = array_aggr,
  array_aggregate = array_aggregate,
  array_append = array_append,
  array_apply = array_apply,
  array_cat = array_cat,
  array_concat = array_concat,
  array_contains = array_contains,
  array_cosine_distance = array_cosine_distance,
  array_cosine_similarity = array_cosine_similarity,
  array_cross_product = array_cross_product,
  array_distance = array_distance,
  array_distinct = array_distinct,
  array_dot_product = array_dot_product,
  array_extract = array_extract,
  array_filter = array_filter,
  array_grade_up = array_grade_up,
  array_has = array_has,
  array_has_all = array_has_all,
  array_has_any = array_has_any,
  array_indexof = array_indexof,
  array_inner_product = array_inner_product,
  array_intersect = array_intersect,
  array_length = array_length,
  array_negative_dot_product = array_negative_dot_product,
  array_negative_inner_product = array_negative_inner_product,
  array_pop_back = array_pop_back,
  array_pop_front = array_pop_front,
  array_position = array_position,
  array_prepend = array_prepend,
  array_push_back = array_push_back,
  array_push_front = array_push_front,
  array_reduce = array_reduce,
  array_resize = array_resize,
  array_reverse = array_reverse,
  array_reverse_sort = array_reverse_sort,
  array_select = array_select,
  array_slice = array_slice,
  array_sort = array_sort,
  array_to_json = array_to_json,
  array_to_string = array_to_string,
  array_to_string_comma_default = array_to_string_comma_default,
  array_transform = array_transform,
  array_unique = array_unique,
  array_value = array_value,
  array_where = array_where,
  array_zip = array_zip,
  arrow_scan = arrow_scan,
  arrow_scan_dumb = arrow_scan_dumb,
  ascii = ascii,
  asin = asin,
  asinh = asinh,
  atan = atan,
  atan2 = atan2,
  atanh = atanh,
  avg = avg,
  bar = bar,
  base64 = base64,
  bin = bin,
  bit_and = bit_and,
  bit_count = bit_count,
  bit_length = bit_length,
  bit_or = bit_or,
  bit_position = bit_position,
  bit_xor = bit_xor,
  bitstring = bitstring,
  bitstring_agg = bitstring_agg,
  bool_and = bool_and,
  bool_or = bool_or,
  broadcast = broadcast,
  can_cast_implicitly = can_cast_implicitly,
  cardinality = cardinality,
  cast_to_type = cast_to_type,
  cbrt = cbrt,
  ceil = ceil,
  ceiling = ceiling,
  century = century,
  char_length = char_length,
  character_length = character_length,
  check_peg_parser = check_peg_parser,
  checkpoint = checkpoint,
  chr = chr,
  col_description = col_description,
  collations = collations,
  combine = combine,
  concat = concat,
  concat_ws = concat_ws,
  constant_or_null = constant_or_null,
  contains = contains,
  copy_database = copy_database,
  copy_dir = copy_dir,
  corr = corr,
  cos = cos,
  cosh = cosh,
  cot = cot,
  count = count,
  count_if = count_if,
  count_star = count_star,
  countif = countif,
  covar_pop = covar_pop,
  covar_samp = covar_samp,
  create_fts_index = create_fts_index,
  create_sort_key = create_sort_key,
  cume_dist = cume_dist,
  current_catalog = current_catalog,
  current_connection_id = current_connection_id,
  current_database = current_database,
  current_date = current_date,
  current_localtime = current_localtime,
  current_localtimestamp = current_localtimestamp,
  current_query = current_query,
  current_query_id = current_query_id,
  current_role = current_role,
  current_schema = current_schema,
  current_schemas = current_schemas,
  current_setting = current_setting,
  current_transaction_id = current_transaction_id,
  current_user = current_user,
  currval = currval,
  damerau_levenshtein = damerau_levenshtein,
  database_list = database_list,
  database_size = database_size,
  date_add = date_add,
  date_diff = date_diff,
  date_part = date_part,
  date_sub = date_sub,
  date_trunc = date_trunc,
  datediff = datediff,
  datepart = datepart,
  datesub = datesub,
  datetrunc = datetrunc,
  day = day,
  dayname = dayname,
  dayofmonth = dayofmonth,
  dayofweek = dayofweek,
  dayofyear = dayofyear,
  days_in_month = days_in_month,
  dbgen = dbgen,
  decade = decade,
  decode = decode,
  degrees = degrees,
  delta_domain_metadata = delta_domain_metadata,
  delta_filter_pushdown_log = delta_filter_pushdown_log,
  delta_filter_pushdown_log_tpcds = delta_filter_pushdown_log_tpcds,
  delta_get_transaction_version = delta_get_transaction_version,
  delta_list_files = delta_list_files,
  delta_scan = delta_scan,
  delta_set_transaction_version = delta_set_transaction_version,
  dense_rank = dense_rank,
  disable_checkpoint_on_shutdown = disable_checkpoint_on_shutdown,
  disable_logging = disable_logging,
  disable_object_cache = disable_object_cache,
  disable_optimizer = disable_optimizer,
  disable_peg_parser = disable_peg_parser,
  disable_print_progress_bar = disable_print_progress_bar,
  disable_profile = disable_profile,
  disable_profiling = disable_profiling,
  disable_progress_bar = disable_progress_bar,
  disable_verification = disable_verification,
  disable_verify_external = disable_verify_external,
  disable_verify_fetch_row = disable_verify_fetch_row,
  disable_verify_parallelism = disable_verify_parallelism,
  disable_verify_serializer = disable_verify_serializer,
  divide = divide,
  drop_fts_index = drop_fts_index,
  dsdgen = dsdgen,
  duckdb_approx_database_count = duckdb_approx_database_count,
  duckdb_columns = duckdb_columns,
  duckdb_connection_count = duckdb_connection_count,
  duckdb_constraints = duckdb_constraints,
  duckdb_coordinate_systems = duckdb_coordinate_systems,
  duckdb_databases = duckdb_databases,
  duckdb_dependencies = duckdb_dependencies,
  duckdb_extensions = duckdb_extensions,
  duckdb_external_file_cache = duckdb_external_file_cache,
  duckdb_functions = duckdb_functions,
  duckdb_indexes = duckdb_indexes,
  duckdb_keywords = duckdb_keywords,
  duckdb_log_contexts = duckdb_log_contexts,
  duckdb_logs = duckdb_logs,
  duckdb_logs_parsed = duckdb_logs_parsed,
  duckdb_memory = duckdb_memory,
  duckdb_optimizers = duckdb_optimizers,
  duckdb_prepared_statements = duckdb_prepared_statements,
  duckdb_profiling_settings = duckdb_profiling_settings,
  duckdb_schemas = duckdb_schemas,
  duckdb_secret_types = duckdb_secret_types,
  duckdb_secrets = duckdb_secrets,
  duckdb_sequences = duckdb_sequences,
  duckdb_settings = duckdb_settings,
  duckdb_table_sample = duckdb_table_sample,
  duckdb_tables = duckdb_tables,
  duckdb_temporary_files = duckdb_temporary_files,
  duckdb_types = duckdb_types,
  duckdb_variables = duckdb_variables,
  duckdb_views = duckdb_views,
  ducklake_add_data_files = ducklake_add_data_files,
  ducklake_cleanup_old_files = ducklake_cleanup_old_files,
  ducklake_commit = ducklake_commit,
  ducklake_current_snapshot = ducklake_current_snapshot,
  ducklake_delete_orphaned_files = ducklake_delete_orphaned_files,
  ducklake_expire_snapshots = ducklake_expire_snapshots,
  ducklake_flush_inlined_data = ducklake_flush_inlined_data,
  ducklake_last_committed_snapshot = ducklake_last_committed_snapshot,
  ducklake_list_files = ducklake_list_files,
  ducklake_merge_adjacent_files = ducklake_merge_adjacent_files,
  ducklake_options = ducklake_options,
  ducklake_rewrite_data_files = ducklake_rewrite_data_files,
  ducklake_scan = ducklake_scan,
  ducklake_set_commit_message = ducklake_set_commit_message,
  ducklake_set_option = ducklake_set_option,
  ducklake_settings = ducklake_settings,
  ducklake_snapshots = ducklake_snapshots,
  ducklake_table_changes = ducklake_table_changes,
  ducklake_table_deletions = ducklake_table_deletions,
  ducklake_table_info = ducklake_table_info,
  ducklake_table_insertions = ducklake_table_insertions,
  editdist3 = editdist3,
  element_at = element_at,
  enable_checkpoint_on_shutdown = enable_checkpoint_on_shutdown,
  enable_object_cache = enable_object_cache,
  enable_optimizer = enable_optimizer,
  enable_peg_parser = enable_peg_parser,
  enable_print_progress_bar = enable_print_progress_bar,
  enable_profile = enable_profile,
  enable_profiling = enable_profiling,
  enable_progress_bar = enable_progress_bar,
  enable_verification = enable_verification,
  encode = encode,
  ends_with = ends_with,
  entropy = entropy,
  enum_code = enum_code,
  enum_first = enum_first,
  enum_last = enum_last,
  enum_range = enum_range,
  enum_range_boundary = enum_range_boundary,
  epoch = epoch,
  epoch_ms = epoch_ms,
  epoch_ns = epoch_ns,
  epoch_us = epoch_us,
  equi_width_bins = equi_width_bins,
  era = era,
  error = error,
  even = even,
  excel_text = excel_text,
  exp = exp,
  extension_versions = extension_versions,
  factorial = factorial,
  family = family,
  favg = favg,
  fdiv = fdiv,
  fill = fill,
  filter = filter,
  finalize = finalize,
  first = first,
  first_value = first_value,
  flatten = flatten,
  floor = floor,
  fmod = fmod,
  force_checkpoint = force_checkpoint,
  formatReadableDecimalSize = formatReadableDecimalSize,
  formatReadableSize = formatReadableSize,
  format_bytes = format_bytes,
  format_pg_type = format_pg_type,
  format_type = format_type,
  from_base64 = from_base64,
  from_binary = from_binary,
  from_hex = from_hex,
  from_json = from_json,
  from_json_strict = from_json_strict,
  fsum = fsum,
  functions = functions,
  gamma = gamma,
  gcd = gcd,
  gen_random_uuid = gen_random_uuid,
  generate_series = generate_series,
  generate_subscripts = generate_subscripts,
  geomean = geomean,
  geometric_mean = geometric_mean,
  get_bit = get_bit,
  get_block_size = get_block_size,
  get_current_time = get_current_time,
  get_current_timestamp = get_current_timestamp,
  get_delta_test_expression = get_delta_test_expression,
  get_type = get_type,
  get_ui_url = get_ui_url,
  getvariable = getvariable,
  glob = glob,
  grade_up = grade_up,
  greatest = greatest,
  greatest_common_divisor = greatest_common_divisor,
  group_concat = group_concat,
  hamming = hamming,
  has_any_column_privilege = has_any_column_privilege,
  has_column_privilege = has_column_privilege,
  has_database_privilege = has_database_privilege,
  has_foreign_data_wrapper_privilege = has_foreign_data_wrapper_privilege,
  has_function_privilege = has_function_privilege,
  has_language_privilege = has_language_privilege,
  has_schema_privilege = has_schema_privilege,
  has_sequence_privilege = has_sequence_privilege,
  has_server_privilege = has_server_privilege,
  has_table_privilege = has_table_privilege,
  has_tablespace_privilege = has_tablespace_privilege,
  hash = hash,
  hex = hex,
  histogram = histogram,
  histogram_exact = histogram_exact,
  histogram_values = histogram_values,
  hnsw_compact_index = hnsw_compact_index,
  hnsw_index_scan = hnsw_index_scan,
  host = host,
  hour = hour,
  html_escape = html_escape,
  html_unescape = html_unescape,
  iceberg_bucket = iceberg_bucket,
  iceberg_column_stats = iceberg_column_stats,
  iceberg_load_table_response = iceberg_load_table_response,
  iceberg_metadata = iceberg_metadata,
  iceberg_partition_stats = iceberg_partition_stats,
  iceberg_scan = iceberg_scan,
  iceberg_schema_properties = iceberg_schema_properties,
  iceberg_snapshots = iceberg_snapshots,
  iceberg_table_properties = iceberg_table_properties,
  iceberg_to_ducklake = iceberg_to_ducklake,
  iceberg_truncate = iceberg_truncate,
  iceberg_verify_equality_deletes = iceberg_verify_equality_deletes,
  icu_calendar_names = icu_calendar_names,
  icu_collate_af = icu_collate_af,
  icu_collate_am = icu_collate_am,
  icu_collate_ar = icu_collate_ar,
  icu_collate_ar_sa = icu_collate_ar_sa,
  icu_collate_as = icu_collate_as,
  icu_collate_az = icu_collate_az,
  icu_collate_be = icu_collate_be,
  icu_collate_bg = icu_collate_bg,
  icu_collate_bn = icu_collate_bn,
  icu_collate_bo = icu_collate_bo,
  icu_collate_br = icu_collate_br,
  icu_collate_bs = icu_collate_bs,
  icu_collate_ca = icu_collate_ca,
  icu_collate_ceb = icu_collate_ceb,
  icu_collate_chr = icu_collate_chr,
  icu_collate_cs = icu_collate_cs,
  icu_collate_cy = icu_collate_cy,
  icu_collate_da = icu_collate_da,
  icu_collate_de = icu_collate_de,
  icu_collate_de_at = icu_collate_de_at,
  icu_collate_dsb = icu_collate_dsb,
  icu_collate_dz = icu_collate_dz,
  icu_collate_ee = icu_collate_ee,
  icu_collate_el = icu_collate_el,
  icu_collate_en = icu_collate_en,
  icu_collate_en_us = icu_collate_en_us,
  icu_collate_eo = icu_collate_eo,
  icu_collate_es = icu_collate_es,
  icu_collate_et = icu_collate_et,
  icu_collate_fa = icu_collate_fa,
  icu_collate_fa_af = icu_collate_fa_af,
  icu_collate_ff = icu_collate_ff,
  icu_collate_fi = icu_collate_fi,
  icu_collate_fil = icu_collate_fil,
  icu_collate_fo = icu_collate_fo,
  icu_collate_fr = icu_collate_fr,
  icu_collate_fr_ca = icu_collate_fr_ca,
  icu_collate_fy = icu_collate_fy,
  icu_collate_ga = icu_collate_ga,
  icu_collate_gl = icu_collate_gl,
  icu_collate_gu = icu_collate_gu,
  icu_collate_ha = icu_collate_ha,
  icu_collate_haw = icu_collate_haw,
  icu_collate_he = icu_collate_he,
  icu_collate_he_il = icu_collate_he_il,
  icu_collate_hi = icu_collate_hi,
  icu_collate_hr = icu_collate_hr,
  icu_collate_hsb = icu_collate_hsb,
  icu_collate_hu = icu_collate_hu,
  icu_collate_hy = icu_collate_hy,
  icu_collate_id = icu_collate_id,
  icu_collate_id_id = icu_collate_id_id,
  icu_collate_ig = icu_collate_ig,
  icu_collate_is = icu_collate_is,
  icu_collate_it = icu_collate_it,
  icu_collate_ja = icu_collate_ja,
  icu_collate_ka = icu_collate_ka,
  icu_collate_kk = icu_collate_kk,
  icu_collate_kl = icu_collate_kl,
  icu_collate_km = icu_collate_km,
  icu_collate_kn = icu_collate_kn,
  icu_collate_ko = icu_collate_ko,
  icu_collate_kok = icu_collate_kok,
  icu_collate_ku = icu_collate_ku,
  icu_collate_ky = icu_collate_ky,
  icu_collate_lb = icu_collate_lb,
  icu_collate_lij = icu_collate_lij,
  icu_collate_lkt = icu_collate_lkt,
  icu_collate_ln = icu_collate_ln,
  icu_collate_lo = icu_collate_lo,
  icu_collate_lt = icu_collate_lt,
  icu_collate_lv = icu_collate_lv,
  icu_collate_mk = icu_collate_mk,
  icu_collate_ml = icu_collate_ml,
  icu_collate_mn = icu_collate_mn,
  icu_collate_mr = icu_collate_mr,
  icu_collate_ms = icu_collate_ms,
  icu_collate_mt = icu_collate_mt,
  icu_collate_my = icu_collate_my,
  icu_collate_nb = icu_collate_nb,
  icu_collate_nb_no = icu_collate_nb_no,
  icu_collate_ne = icu_collate_ne,
  icu_collate_nl = icu_collate_nl,
  icu_collate_nn = icu_collate_nn,
  icu_collate_noaccent = icu_collate_noaccent,
  icu_collate_nso = icu_collate_nso,
  icu_collate_om = icu_collate_om,
  icu_collate_or = icu_collate_or,
  icu_collate_pa = icu_collate_pa,
  icu_collate_pa_in = icu_collate_pa_in,
  icu_collate_pl = icu_collate_pl,
  icu_collate_ps = icu_collate_ps,
  icu_collate_pt = icu_collate_pt,
  icu_collate_ro = icu_collate_ro,
  icu_collate_ru = icu_collate_ru,
  icu_collate_sa = icu_collate_sa,
  icu_collate_se = icu_collate_se,
  icu_collate_si = icu_collate_si,
  icu_collate_sk = icu_collate_sk,
  icu_collate_sl = icu_collate_sl,
  icu_collate_smn = icu_collate_smn,
  icu_collate_sq = icu_collate_sq,
  icu_collate_sr = icu_collate_sr,
  icu_collate_sr_ba = icu_collate_sr_ba,
  icu_collate_sr_me = icu_collate_sr_me,
  icu_collate_sr_rs = icu_collate_sr_rs,
  icu_collate_st = icu_collate_st,
  icu_collate_sv = icu_collate_sv,
  icu_collate_sw = icu_collate_sw,
  icu_collate_ta = icu_collate_ta,
  icu_collate_te = icu_collate_te,
  icu_collate_th = icu_collate_th,
  icu_collate_tk = icu_collate_tk,
  icu_collate_tn = icu_collate_tn,
  icu_collate_to = icu_collate_to,
  icu_collate_tr = icu_collate_tr,
  icu_collate_ug = icu_collate_ug,
  icu_collate_uk = icu_collate_uk,
  icu_collate_ur = icu_collate_ur,
  icu_collate_uz = icu_collate_uz,
  icu_collate_vi = icu_collate_vi,
  icu_collate_wae = icu_collate_wae,
  icu_collate_wo = icu_collate_wo,
  icu_collate_xh = icu_collate_xh,
  icu_collate_yi = icu_collate_yi,
  icu_collate_yo = icu_collate_yo,
  icu_collate_yue = icu_collate_yue,
  icu_collate_yue_cn = icu_collate_yue_cn,
  icu_collate_zh = icu_collate_zh,
  icu_collate_zh_cn = icu_collate_zh_cn,
  icu_collate_zh_hk = icu_collate_zh_hk,
  icu_collate_zh_mo = icu_collate_zh_mo,
  icu_collate_zh_sg = icu_collate_zh_sg,
  icu_collate_zh_tw = icu_collate_zh_tw,
  icu_collate_zu = icu_collate_zu,
  icu_sort_key = icu_sort_key,
  ilike_escape = ilike_escape,
  import_database = import_database,
  in_search_path = in_search_path,
  inet_client_addr = inet_client_addr,
  inet_client_port = inet_client_port,
  inet_server_addr = inet_server_addr,
  inet_server_port = inet_server_port,
  instr = instr,
  is_histogram_other_bin = is_histogram_other_bin,
  isfinite = isfinite,
  isinf = isinf,
  isnan = isnan,
  isodow = isodow,
  isoyear = isoyear,
  jaccard = jaccard,
  jaro_similarity = jaro_similarity,
  jaro_winkler_similarity = jaro_winkler_similarity,
  json = json,
  json_array = json_array,
  json_array_length = json_array_length,
  json_contains = json_contains,
  json_deserialize_sql = json_deserialize_sql,
  json_each = json_each,
  json_execute_serialized_sql = json_execute_serialized_sql,
  json_exists = json_exists,
  json_extract = json_extract,
  json_extract_path = json_extract_path,
  json_extract_path_text = json_extract_path_text,
  json_extract_string = json_extract_string,
  json_group_array = json_group_array,
  json_group_object = json_group_object,
  json_group_structure = json_group_structure,
  json_keys = json_keys,
  json_merge_patch = json_merge_patch,
  json_object = json_object,
  json_pretty = json_pretty,
  json_quote = json_quote,
  json_serialize_plan = json_serialize_plan,
  json_serialize_sql = json_serialize_sql,
  json_structure = json_structure,
  json_transform = json_transform,
  json_transform_strict = json_transform_strict,
  json_tree = json_tree,
  json_type = json_type,
  json_valid = json_valid,
  json_value = json_value,
  julian = julian,
  kahan_sum = kahan_sum,
  kurtosis = kurtosis,
  kurtosis_pop = kurtosis_pop,
  lag = lag,
  lance_fts = lance_fts,
  lance_hybrid_search = lance_hybrid_search,
  lance_vector_search = lance_vector_search,
  last = last,
  last_day = last_day,
  last_value = last_value,
  lcase = lcase,
  lcm = lcm,
  lead = lead,
  least = least,
  least_common_multiple = least_common_multiple,
  left = left,
  left_grapheme = left_grapheme,
  len = len,
  length_grapheme = length_grapheme,
  levenshtein = levenshtein,
  lgamma = lgamma,
  like_escape = like_escape,
  list = list,
  list_aggr = list_aggr,
  list_aggregate = list_aggregate,
  list_any_value = list_any_value,
  list_append = list_append,
  list_apply = list_apply,
  list_approx_count_distinct = list_approx_count_distinct,
  list_avg = list_avg,
  list_bit_and = list_bit_and,
  list_bit_or = list_bit_or,
  list_bit_xor = list_bit_xor,
  list_bool_and = list_bool_and,
  list_bool_or = list_bool_or,
  list_cat = list_cat,
  list_concat = list_concat,
  list_contains = list_contains,
  list_cosine_distance = list_cosine_distance,
  list_cosine_similarity = list_cosine_similarity,
  list_count = list_count,
  list_distance = list_distance,
  list_distinct = list_distinct,
  list_dot_product = list_dot_product,
  list_element = list_element,
  list_entropy = list_entropy,
  list_extract = list_extract,
  list_filter = list_filter,
  list_first = list_first,
  list_grade_up = list_grade_up,
  list_has = list_has,
  list_has_all = list_has_all,
  list_has_any = list_has_any,
  list_histogram = list_histogram,
  list_indexof = list_indexof,
  list_inner_product = list_inner_product,
  list_intersect = list_intersect,
  list_kurtosis = list_kurtosis,
  list_kurtosis_pop = list_kurtosis_pop,
  list_last = list_last,
  list_mad = list_mad,
  list_max = list_max,
  list_median = list_median,
  list_min = list_min,
  list_mode = list_mode,
  list_negative_dot_product = list_negative_dot_product,
  list_negative_inner_product = list_negative_inner_product,
  list_pack = list_pack,
  list_position = list_position,
  list_prepend = list_prepend,
  list_product = list_product,
  list_reduce = list_reduce,
  list_resize = list_resize,
  list_reverse = list_reverse,
  list_reverse_sort = list_reverse_sort,
  list_select = list_select,
  list_sem = list_sem,
  list_skewness = list_skewness,
  list_slice = list_slice,
  list_sort = list_sort,
  list_stddev_pop = list_stddev_pop,
  list_stddev_samp = list_stddev_samp,
  list_string_agg = list_string_agg,
  list_sum = list_sum,
  list_transform = list_transform,
  list_unique = list_unique,
  list_value = list_value,
  list_var_pop = list_var_pop,
  list_var_samp = list_var_samp,
  list_where = list_where,
  list_zip = list_zip,
  listagg = listagg,
  ln = ln,
  load_aws_credentials = load_aws_credentials,
  log = log,
  log10 = log10,
  log2 = log2,
  lower = lower,
  lpad = lpad,
  ltrim = ltrim,
  mad = mad,
  make_date = make_date,
  make_time = make_time,
  make_timestamp = make_timestamp,
  make_timestamp_ms = make_timestamp_ms,
  make_timestamp_ns = make_timestamp_ns,
  make_timestamptz = make_timestamptz,
  make_type = make_type,
  map = map,
  map_concat = map_concat,
  map_contains = map_contains,
  map_contains_entry = map_contains_entry,
  map_contains_value = map_contains_value,
  map_entries = map_entries,
  map_extract = map_extract,
  map_extract_value = map_extract_value,
  map_from_entries = map_from_entries,
  map_keys = map_keys,
  map_to_pg_oid = map_to_pg_oid,
  map_values = map_values,
  max = max,
  max_by = max_by,
  md5 = md5,
  md5_number = md5_number,
  md5_number_lower = md5_number_lower,
  md5_number_upper = md5_number_upper,
  mean = mean,
  median = median,
  metadata_info = metadata_info,
  microsecond = microsecond,
  millennium = millennium,
  millisecond = millisecond,
  min = min,
  min_by = min_by,
  minute = minute,
  mismatches = mismatches,
  mod = mod,
  mode = mode,
  month = month,
  monthname = monthname,
  multiply = multiply,
  murmur3_32 = murmur3_32,
  mysql_clear_cache = mysql_clear_cache,
  mysql_debug_execution_plan = mysql_debug_execution_plan,
  mysql_execute = mysql_execute,
  mysql_explain_federated = mysql_explain_federated,
  mysql_query = mysql_query,
  nanosecond = nanosecond,
  netmask = netmask,
  network = network,
  nextafter = nextafter,
  nextval = nextval,
  nfc_normalize = nfc_normalize,
  normalized_interval = normalized_interval,
  not_ilike_escape = not_ilike_escape,
  not_like_escape = not_like_escape,
  now = now,
  nth_value = nth_value,
  ntile = ntile,
  nullif = nullif,
  obj_description = obj_description,
  octet_length = octet_length,
  ord = ord,
  parquet_bloom_probe = parquet_bloom_probe,
  parquet_file_metadata = parquet_file_metadata,
  parquet_full_metadata = parquet_full_metadata,
  parquet_kv_metadata = parquet_kv_metadata,
  parquet_metadata = parquet_metadata,
  parquet_scan = parquet_scan,
  parquet_schema = parquet_schema,
  parse_delta_filter_logline = parse_delta_filter_logline,
  parse_dirname = parse_dirname,
  parse_dirpath = parse_dirpath,
  parse_duckdb_log_message = parse_duckdb_log_message,
  parse_filename = parse_filename,
  parse_formatted_bytes = parse_formatted_bytes,
  parse_path = parse_path,
  percent_rank = percent_rank,
  pg_clear_cache = pg_clear_cache,
  pg_collation_is_visible = pg_collation_is_visible,
  pg_conf_load_time = pg_conf_load_time,
  pg_conversion_is_visible = pg_conversion_is_visible,
  pg_function_is_visible = pg_function_is_visible,
  pg_get_constraintdef = pg_get_constraintdef,
  pg_get_expr = pg_get_expr,
  pg_get_viewdef = pg_get_viewdef,
  pg_has_role = pg_has_role,
  pg_is_other_temp_schema = pg_is_other_temp_schema,
  pg_my_temp_schema = pg_my_temp_schema,
  pg_opclass_is_visible = pg_opclass_is_visible,
  pg_operator_is_visible = pg_operator_is_visible,
  pg_opfamily_is_visible = pg_opfamily_is_visible,
  pg_postmaster_start_time = pg_postmaster_start_time,
  pg_size_pretty = pg_size_pretty,
  pg_sleep = pg_sleep,
  pg_table_is_visible = pg_table_is_visible,
  pg_timezone_names = pg_timezone_names,
  pg_ts_config_is_visible = pg_ts_config_is_visible,
  pg_ts_dict_is_visible = pg_ts_dict_is_visible,
  pg_ts_parser_is_visible = pg_ts_parser_is_visible,
  pg_ts_template_is_visible = pg_ts_template_is_visible,
  pg_type_is_visible = pg_type_is_visible,
  pg_typeof = pg_typeof,
  pi = pi,
  platform = platform,
  position = position,
  postgres_attach = postgres_attach,
  postgres_configure_pool = postgres_configure_pool,
  postgres_execute = postgres_execute,
  postgres_hstore_get = postgres_hstore_get,
  postgres_hstore_to_json = postgres_hstore_to_json,
  postgres_query = postgres_query,
  postgres_scan = postgres_scan,
  postgres_scan_pushdown = postgres_scan_pushdown,
  pow = pow,
  power = power,
  pragma_collations = pragma_collations,
  pragma_database_size = pragma_database_size,
  pragma_hnsw_index_info = pragma_hnsw_index_info,
  pragma_metadata_info = pragma_metadata_info,
  pragma_platform = pragma_platform,
  pragma_rtree_index_info = pragma_rtree_index_info,
  pragma_show = pragma_show,
  pragma_storage_info = pragma_storage_info,
  pragma_table_info = pragma_table_info,
  pragma_user_agent = pragma_user_agent,
  pragma_version = pragma_version,
  prefix = prefix,
  printf = printf,
  product = product,
  quack_active_connections = quack_active_connections,
  quack_check_token = quack_check_token,
  quack_clear_cache = quack_clear_cache,
  quack_identify = quack_identify,
  quack_nop_authorization = quack_nop_authorization,
  quack_query = quack_query,
  quack_query_by_name = quack_query_by_name,
  quack_serve = quack_serve,
  quack_server_list = quack_server_list,
  quack_stop = quack_stop,
  quack_uri_parser = quack_uri_parser,
  quantile = quantile,
  quantile_cont = quantile_cont,
  quantile_disc = quantile_disc,
  quarter = quarter,
  query = query,
  query_table = query_table,
  r_dataframe_scan = r_dataframe_scan,
  radians = radians,
  random = random,
  range = range,
  rank = rank,
  rank_dense = rank_dense,
  read_avro = read_avro,
  read_blob = read_blob,
  read_csv = read_csv,
  read_csv_auto = read_csv_auto,
  read_duckdb = read_duckdb,
  read_json = read_json,
  read_json_auto = read_json_auto,
  read_json_objects = read_json_objects,
  read_json_objects_auto = read_json_objects_auto,
  read_ndjson = read_ndjson,
  read_ndjson_auto = read_ndjson_auto,
  read_ndjson_objects = read_ndjson_objects,
  read_parquet = read_parquet,
  read_postgres_binary = read_postgres_binary,
  read_text = read_text,
  read_vortex = read_vortex,
  read_xlsx = read_xlsx,
  reduce = reduce,
  regexp_escape = regexp_escape,
  regexp_extract = regexp_extract,
  regexp_extract_all = regexp_extract_all,
  regexp_full_match = regexp_full_match,
  regexp_matches = regexp_matches,
  regexp_replace = regexp_replace,
  regexp_split_to_array = regexp_split_to_array,
  regexp_split_to_table = regexp_split_to_table,
  regr_avgx = regr_avgx,
  regr_avgy = regr_avgy,
  regr_count = regr_count,
  regr_intercept = regr_intercept,
  regr_r2 = regr_r2,
  regr_slope = regr_slope,
  regr_sxx = regr_sxx,
  regr_sxy = regr_sxy,
  regr_syy = regr_syy,
  remove_iceberg_schema_properties = remove_iceberg_schema_properties,
  remove_iceberg_table_properties = remove_iceberg_table_properties,
  `repeat` = `repeat`,
  repeat_row = repeat_row,
  replace = replace,
  replace_type = replace_type,
  reservoir_quantile = reservoir_quantile,
  reverse = reverse,
  right = right,
  right_grapheme = right_grapheme,
  round = round,
  round_even = round_even,
  roundbankers = roundbankers,
  row = row,
  row_number = row_number,
  row_to_json = row_to_json,
  rpad = rpad,
  rtree_index_dump = rtree_index_dump,
  rtree_index_scan = rtree_index_scan,
  rtrim = rtrim,
  second = second,
  sem = sem,
  seq_scan = seq_scan,
  session_user = session_user,
  set_bit = set_bit,
  set_iceberg_schema_properties = set_iceberg_schema_properties,
  set_iceberg_table_properties = set_iceberg_table_properties,
  setseed = setseed,
  sha1 = sha1,
  sha256 = sha256,
  shapefile_meta = shapefile_meta,
  shobj_description = shobj_description,
  show = show,
  show_databases = show_databases,
  show_tables = show_tables,
  show_tables_expanded = show_tables_expanded,
  sign = sign,
  signbit = signbit,
  sin = sin,
  sinh = sinh,
  skewness = skewness,
  sleep_ms = sleep_ms,
  split = split,
  split_part = split_part,
  sql_auto_complete = sql_auto_complete,
  sqlite_attach = sqlite_attach,
  sqlite_query = sqlite_query,
  sqlite_scan = sqlite_scan,
  sqrt = sqrt,
  st_asbinary = st_asbinary,
  st_astext = st_astext,
  st_aswkb = st_aswkb,
  st_aswkt = st_aswkt,
  st_crs = st_crs,
  st_geomfromwkb = st_geomfromwkb,
  st_intersects_extent = st_intersects_extent,
  st_setcrs = st_setcrs,
  start_ui = start_ui,
  start_ui_server = start_ui_server,
  starts_with = starts_with,
  stats = stats,
  stddev = stddev,
  stddev_pop = stddev_pop,
  stddev_samp = stddev_samp,
  stem = stem,
  stop_ui_server = stop_ui_server,
  storage_info = storage_info,
  str_split = str_split,
  str_split_regex = str_split_regex,
  strftime = strftime,
  string_agg = string_agg,
  string_split = string_split,
  string_split_regex = string_split_regex,
  string_to_array = string_to_array,
  strip_accents = strip_accents,
  strlen = strlen,
  strpos = strpos,
  strptime = strptime,
  struct_concat = struct_concat,
  struct_contains = struct_contains,
  struct_extract = struct_extract,
  struct_has = struct_has,
  struct_indexof = struct_indexof,
  struct_insert = struct_insert,
  struct_keys = struct_keys,
  struct_pack = struct_pack,
  struct_position = struct_position,
  struct_update = struct_update,
  struct_values = struct_values,
  substr = substr,
  substring = substring,
  substring_grapheme = substring_grapheme,
  subtract = subtract,
  suffix = suffix,
  sum = sum,
  sum_no_overflow = sum_no_overflow,
  sumkahan = sumkahan,
  summary = summary,
  switch = switch,
  table_info = table_info,
  tan = tan,
  tanh = tanh,
  test_all_types = test_all_types,
  test_vector_types = test_vector_types,
  text = text,
  time_bucket = time_bucket,
  timetz_byte_comparable = timetz_byte_comparable,
  timezone = timezone,
  timezone_hour = timezone_hour,
  timezone_minute = timezone_minute,
  to_base = to_base,
  to_base64 = to_base64,
  to_binary = to_binary,
  to_centuries = to_centuries,
  to_days = to_days,
  to_decades = to_decades,
  to_hex = to_hex,
  to_hours = to_hours,
  to_json = to_json,
  to_microseconds = to_microseconds,
  to_millennia = to_millennia,
  to_milliseconds = to_milliseconds,
  to_minutes = to_minutes,
  to_months = to_months,
  to_quarters = to_quarters,
  to_seconds = to_seconds,
  to_timestamp = to_timestamp,
  to_weeks = to_weeks,
  to_years = to_years,
  today = today,
  tpcds = tpcds,
  tpcds_answers = tpcds_answers,
  tpcds_queries = tpcds_queries,
  tpch = tpch,
  tpch_answers = tpch_answers,
  tpch_queries = tpch_queries,
  transaction_timestamp = transaction_timestamp,
  translate = translate,
  trim = trim,
  trunc = trunc,
  truncate_duckdb_logs = truncate_duckdb_logs,
  try_strptime = try_strptime,
  txid_current = txid_current,
  typeof = typeof,
  ucase = ucase,
  ui_is_started = ui_is_started,
  unbin = unbin,
  unhex = unhex,
  unicode = unicode,
  union_extract = union_extract,
  union_tag = union_tag,
  union_value = union_value,
  unity_catalog_checkpoint_table = unity_catalog_checkpoint_table,
  unity_catalog_force_checkpoint_table = unity_catalog_force_checkpoint_table,
  unnest = unnest,
  unpivot_list = unpivot_list,
  upper = upper,
  url_decode = url_decode,
  url_encode = url_encode,
  user = user,
  user_agent = user_agent,
  uuid = uuid,
  uuid_extract_timestamp = uuid_extract_timestamp,
  uuid_extract_version = uuid_extract_version,
  uuidv4 = uuidv4,
  uuidv7 = uuidv7,
  var_pop = var_pop,
  var_samp = var_samp,
  variance = variance,
  variant_bytes_to_variant = variant_bytes_to_variant,
  variant_extract = variant_extract,
  variant_normalize = variant_normalize,
  variant_to_parquet_variant = variant_to_parquet_variant,
  variant_typeof = variant_typeof,
  vector_type = vector_type,
  verify_external = verify_external,
  verify_fetch_row = verify_fetch_row,
  verify_parallelism = verify_parallelism,
  verify_serializer = verify_serializer,
  version = version,
  vortex_scan = vortex_scan,
  vss_join = vss_join,
  vss_match = vss_match,
  wavg = wavg,
  week = week,
  weekday = weekday,
  weekofyear = weekofyear,
  weighted_avg = weighted_avg,
  which_secret = which_secret,
  whoami = whoami,
  write_blob = write_blob,
  write_log = write_log,
  xor = xor,
  year = year,
  yearweek = yearweek,
  `|` = `|`,
  `||` = `||`,
  `~` = `~`,
  `~~` = `~~`,
  `~~*` = `~~*`,
  `~~~` = `~~~`
)
